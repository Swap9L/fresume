import { getResumeData } from "@/lib/actions";

export async function addWork(data: any) {
    // Scaffold for adding Work Experiences
}

export async function deleteWork(id: string) {
    // Scaffold for deleting Work Experiences
}

export async function addHackathon(data: any) {
    // Scaffold for adding Hackathons
}

export async function deleteHackathon(id: string) {
    // Scaffold for deleting Hackathons
}
